# BASim

This repository contains the Building Automation Simulator project.

## Structure

- **campus-sim/**: The main simulator application (Python/Flask)

## Getting Started

To run the simulator:

```bash
cd campus-sim
docker compose up -d --build
```

Or see `campus-sim/README.md` for more details.
